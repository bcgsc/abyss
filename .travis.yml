language: cpp
compiler:
  - gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libboost-dev libgtest-dev libopenmpi-dev libsparsehash-dev zlib1g-dev
script:
  - ./autogen.sh
  - ./configure CXXFLAGS=-std=c++11 --with-mpi=/usr/lib/openmpi
  - make
  - make check
